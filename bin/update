#!/bin/bash
set -o errexit
PROJECT_NAME=$(basename $(pwd))
git checkout HEAD --force
git pull --force
sed -e "s/project_name/$PROJECT_NAME/g" docker-compose.yml >docker-compose.yml.new &&
    mv -f docker-compose.yml.new docker-compose.yml
